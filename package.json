{
    "name": "pyipaddr",
    "version": "0.0.1",
    "scripts": {
        "test": "tsc && mocha tests/*.js",
        "build": "tsc && babel src/*.js -d lib"
    },
    "devDependencies": {
        "babel-cli": "^6.24.1",
        "babel-preset-env": "^1.5.1",
        "mocha": "^3.4.2"
    }
}
