{
    "name": "pyipaddr",
    "version": "0.0.1",
    "scripts": {
        "lint": "tslint -c tslint.json -p tsconfig.json --type-check",
        "test": "yarn run build && mocha dist/tests/*.js",
        "build": "yarn run clean && tsc --outDir dist/",
        "clean": "rm -rf dist/"
    },
    "main": "./dist/src/index.js",
    "types": "./dist/src/index.d.ts",
    "devDependencies": {
        "@types/mocha": "^2.2.43",
        "@types/node": "^8.0.47",
        "babel-cli": "^6.24.1",
        "babel-preset-env": "^1.5.1",
        "mocha": "^3.4.2",
        "tslint": "^5.4.3",
        "typescript": "^2.3.4"
    }
}
